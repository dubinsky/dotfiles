# source_up

export ORG_GRADLE_PROJECT_mavenCentralUsername=$(cat ./Keys/mavenCentralUsername)
export ORG_GRADLE_PROJECT_mavenCentralPassword=$(cat ./Keys/mavenCentralPassword)
export ORG_GRADLE_PROJECT_signingKey=$(cat ./Keys/gnupg.dub-podval-org.key)
export ORG_GRADLE_PROJECT_signingPassword=$(cat ./Keys/gnupg.dub-podval-org.password)
# TODO use normal environment variables; get them with System.getenv('...')
export GRADLE_SIGNING_KEY=$(cat ./Keys/gnupg.dub-podval-org.key)
export GRADLE_SIGNING_PASSWORD=$(cat ./Keys/gnupg.dub-podval-org.password)
export GRADLE_PUBLISH_KEY=$(cat ./Keys/gradle.publish.key)
export GRADLE_PUBLISH_SECRET=$(cat ./Keys/gradle.publish.secret)

export DEVPOD_CONTEXT="box"

# TODO make gcloud provider use DEVPOD_PROVIDER_GCLOUD_key or something
# export GOOGLE_APPLICATION_CREDENTIALS="../Keys/devpod-podval-devpod.json"

export DEVPOD_PROVIDER_GCLOUD_PROJECT="podval-devpod"
export DEVPOD_PROVIDER_GCLOUD_SERVICE_ACCOUNT="devpod@podval-devpod.iam.gserviceaccount.com"
export DEVPOD_PROVIDER_GCLOUD_ZONE="us-east4-b"
export DEVPOD_PROVIDER_GCLOUD_MACHINE_TYPE="c2-standard-8"
export DEVPOD_PROVIDER_GCLOUD_DISK_SIZE=5
