#!/bin/bash

# JetBrains IDEs are available from AUR,
# but the best way to install and update them is via JetBrains Toolbox,
# which is also available from AUR :)
yay -S --needed --noconfirm jetbrains-toolbox
